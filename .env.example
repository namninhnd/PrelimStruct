# =============================================================================
# PrelimStruct AI Configuration
# =============================================================================
# Copy this file to .env and fill in your API key

# Provider: gemini (default), deepseek, grok, openrouter
LLM_PROVIDER=gemini

# Google AI Studio API key (get one at https://aistudio.google.com/apikey)
GEMINI_API_KEY=your-google-ai-studio-api-key-here

# Optional: Model override (default: gemini-3-flash-preview)
# LLM_MODEL=gemini-3-flash-preview

# Optional: Request timeout in seconds (default: 60)
# LLM_TIMEOUT=60

# Optional: Max retry attempts (default: 3)
# LLM_MAX_RETRIES=3

# Optional: Cost tracking (default: true)
# LLM_TRACK_COSTS=true

# Optional: Monthly budget limit in USD (default: 0 = unlimited)
# LLM_MONTHLY_BUDGET=0

# --- Backup Providers (uncomment to use) ---
# DEEPSEEK_API_KEY=your-deepseek-key
# GROK_API_KEY=your-grok-key
# OPENROUTER_API_KEY=your-openrouter-key
