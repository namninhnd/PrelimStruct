[pytest]
# Force pytest to use a writable temp directory inside the repo to avoid permission issues
addopts = --basetemp=.pytest_tmp

# Register custom marks
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
